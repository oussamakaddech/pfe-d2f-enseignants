# ruff
extend-exclude = ["app_legacy", ".venv", "tests"]
line-length = 120
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = true
exclude = ["app_legacy/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
