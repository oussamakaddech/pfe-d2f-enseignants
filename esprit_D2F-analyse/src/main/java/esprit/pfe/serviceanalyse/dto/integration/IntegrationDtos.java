package esprit.pfe.serviceanalyse.dto.integration;

/**
 * Typed DTOs for downstream D2F services live in this package as public records
 * (one per file) so the {@code client} package can deserialize them. This file
 * is intentionally empty of types.
 */
