package esprit.pfe.serviceformation.entities;

/**
 * Origine de la proposition d'animation.
 */
public enum ProposalType {
    /** Proposition émise par un responsable (CUP / ADMIN / CHEF_DEPARTEMENT). */
    MANAGER_PROPOSAL,
    /** Auto-proposition par l'enseignant ou l'animateur lui-même. */
    SELF_PROPOSAL
}
