node_modules/
.vscode/
__pycache__/
*.pyc
_semantic_cache/
*.log
diag_out.txt
actions-runner/
**/target/
*.zip

# ── Claude Code agent worktrees (copies transitoires, secrets/clutter) ────────
.claude/worktrees/

# ── Secrets & environment files (DSI §1.1 — jamais committer) ─────────────────
.env
.env.local
.env.prod
.env.qa
.env.*
!.env.example

# ── Fichiers de build / compilation ───────────────────────────────────────────
**/dist/
**/.pytest_cache/

# ── Fichiers temporaires / backup ─────────────────────────────────────────────
*.old
*.bak
*.new
rice_analyzer_backup.py
run.log
diag_out.txt
debug_ens.py
diag_tv.py

# Large build artifacts
formation-service.jar

# ── Artefacts de compilation LaTeX (rapport PFE) ─────────────────────────────
# On versionne les sources (.tex/.bib) mais pas les fichiers intermédiaires.
rapport-pfe/*.aux
rapport-pfe/*.bbl
rapport-pfe/*.bcf
rapport-pfe/*.blg
rapport-pfe/*.fdb_latexmk
rapport-pfe/*.fls
rapport-pfe/*.lof
rapport-pfe/*.lot
rapport-pfe/*.out
rapport-pfe/*.run.xml
rapport-pfe/*.synctex.gz
rapport-pfe/*.toc
