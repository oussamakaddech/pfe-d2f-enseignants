node_modules/
.vscode/
__pycache__/
*.pyc
_semantic_cache/
*.log
diag_out.txt
actions-runner/
**/target/
*.zip

# ── Claude Code agent worktrees (copies transitoires, secrets/clutter) ────────
.claude/worktrees/

# ── Secrets & environment files (DSI §1.1 — jamais committer) ─────────────────
.env
.env.local
.env.prod
.env.qa
.env.*
!.env.example

# ── Fichiers de build / compilation ───────────────────────────────────────────
**/dist/
**/.pytest_cache/

# ── Fichiers temporaires / backup ─────────────────────────────────────────────
*.old
*.bak
*.new
rice_analyzer_backup.py
run.log
diag_out.txt
debug_ens.py
diag_tv.py

# Large build artifacts
formation-service.jar

# ── Cookies & sessions (jamais committer — fuite de credentials) ─────────────
cookies.txt
cookies2.txt
cookies_admin.txt
cookies_*.txt

# ── Dumps de base de données (données personnelles) ──────────────────────────
dump_*.sql
*.dump

# ── Artefacts parasites Windows (redirection NUL) ────────────────────────────
nul

# ── Fichiers de listing temporaires ──────────────────────────────────────────
_allfiles.txt

# ── Rapport PFE ────────────────────────────────────────────────────────────────
rapport-pfe/
