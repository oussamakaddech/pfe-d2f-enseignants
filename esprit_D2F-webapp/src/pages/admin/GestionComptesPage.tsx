import ListAccounts from '@/pages/admin/gererComptes/ListAccounts';

/**
 * Page dédiée à la gestion des comptes d'accès.
 * Administration des comptes utilisateurs, rôles et statuts (actif / bloqué).
 */
export default function GestionComptesPage() {
  return <ListAccounts />;
}
