fastapi
uvicorn[standard]
psycopg2-binary
pdfplumber
python-docx
python-multipart
python-dotenv
# Optional – improved name matching (Point 5)
rapidfuzz
# Optional – semantic GC matching (Point 2); pulls ~80 MB model on first use
sentence-transformers
# Optional – LLM-powered extraction (Ollama local model)
ollama
# Optional – OCR for scanned/image PDFs (requires Tesseract binary in PATH or TESSERACT_CMD env var)
# Install Tesseract: https://github.com/UB-Mannheim/tesseract/wiki
# Also install the French language pack (fra.traineddata) for ESPRIT fiches
pymupdf
pytesseract
